import LoadingScreen from '@/components/LoadingScreen'

// Affiché automatiquement par Next.js pendant le chargement des pages (Suspense).
export default function Loading() {
  return <LoadingScreen />
}
